# ChamCard API (Render/Railway/VPS)
# Example values only. Do NOT commit real secrets.

PORT=8080
DATA_DIR=./data

# Required: used by /api/admin/* (x-admin-token header)
ADMIN_TOKEN=REPLACE_WITH_LONG_RANDOM

# Required: restrict admin access to your own public IPv4 (comma-separated)
# Example: ADMIN_ALLOWED_IPS=78.163.119.144,95.43.120.18
ADMIN_ALLOWED_IPS=78.163.119.144

# Required: HTTP Basic Auth in front of admin pages/APIs
ADMIN_BASIC_USER=admin
ADMIN_BASIC_PASS=REPLACE_WITH_STRONG_PASSWORD

# Required in production: only allow your frontends
CORS_ORIGINS=https://your-chamcard-site.example,https://your-validator-site.example
